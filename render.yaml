services:
  - type: web
    name: ecommerce-api
    env: python
    buildCommand: ./build.sh
    startCommand: cd backend && uvicorn server:app --host 0.0.0.0 --port 8000
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
